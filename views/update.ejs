<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end">
            <a class="bg-red-500 px-2 py-1 text-sm rounded-md inline-block" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5 text-sm ">You can create a new post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea class="resize-none p-3 rounded-md block border-2 border-zinc-800 outline-none w-1/2" name="content" placeholder="What's in your mind ?"><%= post.content %></textarea>
            <input class="bg-yellow-500 block px-3 py-2 text-black text-sm rounded-md w-40 mt-3" type="submit" value="Update post"> 
        </form>

    </div>
</body>
</html>